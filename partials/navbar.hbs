<header>

   <button class="theme-toggle" type="button" data-theme-toggle aria-pressed="false" aria-label="Zum Dark Mode wechseln">
      <span class="theme-toggle__icon theme-toggle__icon--sun" aria-hidden="true">☀️</span>
      <span class="theme-toggle__icon theme-toggle__icon--moon" aria-hidden="true" hidden>🌙</span>
   </button>

   {{! @website global variable used to display a logo and website name  }}
   {{! check if a Website logo exists, if not, dispaly the website name }}
   <a id="logo" href="{{@website.url}}">
      {{#if @website.logo}}
         <img src="{{@website.logo}}" alt="{{@website.name}}">
      {{else}}
         {{@website.name}}
      {{/if}}
   </a>
   {{! /check if a Website logo exists, if not, dispaly the website name }}
   {{! /@website global variable used to display a logo and website name  }}

   {{! check if a menu is assigned to the Main menu position }}
   {{#if menus.mainMenu}}
      {{> menu menus.mainMenu}}
   {{/if}}
   {{! /check if a menu is assigned to the Main menu position }}

   {{! display search plugin (searchbox: input + button) }}
      {{{@customSearchInput}}} 
   {{! /display search plugin (searchbox: input + button) }}

</header>